database {
  jdbcUrl = "jdbc:postgresql://localhost:5432/pcast_test"
  driver = "org.postgresql.Driver"
  user = "pcast"
  password = "pcast"
}

jwt {
  secret = "test-secret-key-for-unit-tests-min-32-characters"
  issuer = "pcast-api"
  audience = "pcast-api"
  accessTokenExpireMinutes = 60
  refreshTokenExpireDays = 30
}
